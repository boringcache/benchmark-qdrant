workspace = "boringcache/benchmark-qdrant"

# Docker BuildKit registry cache benchmark. The OCI cache is the only measured
# BoringCache surface, so there are no archive entries. Qdrant's upstream
# cargo-chef Dockerfile remains untouched.
